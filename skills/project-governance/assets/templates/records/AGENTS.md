# {{PROJECT_NAME}} Agent Instructions

This repository is a maintained document or data collection.

- Preserve source records and their meaning; do not normalize or rewrite data
  without a task-specific reason.
- Keep generated, temporary, cached, secret, and personal runtime files out of
  Git unless a sanitized artifact is deliberately part of the source record.
- Update `README.md` and `docs/HANDOFF.md` when the collection's structure,
  conventions, or maintenance procedure changes.
- Use Git commits for exact history. Do not invent semantic releases when the
  collection has no release concept.
